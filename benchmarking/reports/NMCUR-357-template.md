# NMCUR-357: jusText CJK Boilerplate Study

Status: awaiting the interactive GPU canary and full run. The final report is generated by
`benchmarking/scripts/nmcur357_analysis.py` only after all validation gates pass.

## Required run provenance

- Curator commit/version
- Exact commands and pinned Curator/vLLM/Dynamo/Ray versions
- Allocation and job-step IDs
- Explicit Ray, model-server, worker, concurrency, object-store, and stage-resource configuration
- All three fixed canary trial rates and their spread
- Measured full-run duration and throughput; any canary projection clearly marked as an estimate

## Required measurements

- Exact cohort and per-language counts
- Unique, identical input/output document-ID sets
- MinerU status distribution and conversion-error rate
- Micro totals and document p10/median/p90 by language and population-weighted non-spaced aggregate
- 1,000-replicate document-cluster bootstrap intervals with seed 357
- jusText boilerplate, main, ambiguous, and unmatched shares
- MinerU-other retention by jusText and excess jusText length explained by MinerU `other`
- Pinned GPT-Neo token totals as a secondary dashboard-comparison metric
- Fifty reviewed examples per language, five from every document-ratio decile

## Interpretation boundary

MinerU labels are the reference classifier for this study, not human ground truth. Only
`_mineru_status == "ok"` rows contribute to primary ratios; fallback and error rows remain in the status report.

## Expected artifacts

- `per_document_metrics.parquet`
- `summary.json`
- `summary.csv`
- `review_examples.jsonl`
- `NMCUR-357-report.md`
- `_SUCCESS.json`, written only after schemas, counts, IDs, footers, sampled Zstandard cells, checkpoint completion,
  and failed-task-marker absence validate
